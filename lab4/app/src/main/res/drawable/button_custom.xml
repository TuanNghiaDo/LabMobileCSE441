<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"> <!-- Hình dạng cơ bản là hình chữ nhật -->

    <!-- Màu nền của Button -->
    <solid android:color="@color/btnBackground" />

    <!-- Padding (Tùy chọn - Khoảng cách từ viền vào nội dung text) -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>